<h1>Post grade for <%=User.find_by(id: @enrollment.user_id).name %></h1>

<%= form_for(@enrollment) do |f| %>
    <% if @enrollment.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@enrollment.errors.count, "error") %> prohibited this enrollment from being saved:</h2>

          <ul>
            <% @enrollment.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <!--    <div class="field">
      <%= f.label :course_number %><br>
      <%= f.collection_select(:course_id, Course.all, :id, :title, {} ) %>
    </div>-->

    <div class="field">
      <%= f.label :grade %>
      <%= f.text_field :grade %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>

<%= link_to 'Show', @enrollment %> |
<%= link_to 'Back', enrollments_path %>
